{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for ZeroClaw Desktop",
  "windows": ["chat"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
